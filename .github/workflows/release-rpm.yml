---
name: Build and Release .rpm Package
on:
  workflow_dispatch:
    inputs:
      version:
        description: "Version to build (e.g., 0.1.0)"
        required: true
        type: string
      upload_to_release:
        description: "Upload .rpm to GitHub Release (requires existing tag)"
        required: false
        type: boolean
        default: false
  workflow_run:
    workflows: ["Build and Release Binaries"]
    types: [completed]
permissions:
  contents: write
jobs:
  build-rpm:
    if: >-
      github.event_name == 'workflow_dispatch' ||
      (github.event.workflow_run.conclusion == 'success' &&
       github.event.workflow_run.event == 'release')
    strategy:
      fail-fast: false
      matrix:
        include:
          - arch: x86_64
            binary_arch: x86_64
          - arch: aarch64
            binary_arch: arm64
    runs-on: ubuntu-latest
    steps:
      - name: Checkout source code
        uses: actions/checkout@v6
        with:
          ref: ${{ github.event_name == 'workflow_dispatch' && format('v{0}', github.event.inputs.version) || github.event.workflow_run.head_branch }}

      - name: Get Version
        id: get_version
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            RAW_VERSION="${{ github.event.inputs.version }}"
          else
            RAW_VERSION="${{ github.event.workflow_run.head_branch }}"
          fi
          VERSION="${RAW_VERSION#v}"
          echo "VERSION=$VERSION" >> $GITHUB_ENV
          echo "Resolved version: $VERSION"

      - name: Download prebuilt binary
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release download "v${{ env.VERSION }}" \
            --pattern "doma-v${{ env.VERSION }}-linux-${{ matrix.binary_arch }}" \
            --output doma
          chmod +x doma

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: "stable"

      - name: Install nfpm
        run: |
          go install github.com/goreleaser/nfpm/v2/cmd/nfpm@latest

      - name: Create nfpm config and build .rpm
        # Static musl binary → no runtime depends. Same reason we don't
        # declare them on the .deb side.
        run: |
          cat > nfpm.yaml <<EOF
          name: doma
          arch: ${{ matrix.arch }}
          version: ${{ env.VERSION }}
          maintainer: HAHWUL <hahwul@gmail.com>
          description: "Directory tag manager — put your directories on the cutting board (Crystal CLI)."
          license: MIT
          contents:
            - src: doma
              dst: /usr/bin/doma
              file_info:
                mode: 0755
            - src: LICENSE
              dst: /usr/share/licenses/doma/LICENSE
              file_info:
                mode: 0644
          EOF
          sed -i 's/^          //' nfpm.yaml
          nfpm package --packager rpm --target doma-${{ env.VERSION }}.${{ matrix.arch }}.rpm

      - name: Upload artifacts
        uses: actions/upload-artifact@v6
        with:
          name: doma-${{ env.VERSION }}.${{ matrix.arch }}.rpm
          path: doma-${{ env.VERSION }}.${{ matrix.arch }}.rpm

      - name: Upload .rpm to Release
        if: github.event_name == 'workflow_run' || (github.event_name == 'workflow_dispatch' && inputs.upload_to_release)
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release upload "v${{ env.VERSION }}" \
            "doma-${{ env.VERSION }}.${{ matrix.arch }}.rpm" --clobber
