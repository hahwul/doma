# typed: strict
# frozen_string_literal: true

# This file is rendered by doma's release pipeline (.github/workflows/publish-homebrew.yml).
# DO NOT EDIT by hand.
class Doma < Formula
  desc "Directory tag manager - put your directories on the cutting board"
  homepage "https://github.com/hahwul/doma"
  version "__VERSION__"
  license "MIT"

  # doma ships prebuilt binaries: Linux is statically linked (musl) and macOS
  # links only system libraries (system libsqlite3) plus statically linked
  # Crystal runtime libs, so the formula declares no dependencies. An otool
  # guard in release-binary.yml enforces that no Homebrew dylib leaks in.
  on_macos do
    on_arm do
      url "https://github.com/hahwul/doma/releases/download/v__VERSION__/doma-v__VERSION__-osx-arm64"
      sha256 "__SHA_OSX_ARM64__"
    end
    on_intel do
      url "https://github.com/hahwul/doma/releases/download/v__VERSION__/doma-v__VERSION__-osx-x86_64"
      sha256 "__SHA_OSX_X86_64__"
    end
  end

  on_linux do
    on_arm do
      url "https://github.com/hahwul/doma/releases/download/v__VERSION__/doma-v__VERSION__-linux-arm64"
      sha256 "__SHA_LINUX_ARM64__"
    end
    on_intel do
      url "https://github.com/hahwul/doma/releases/download/v__VERSION__/doma-v__VERSION__-linux-x86_64"
      sha256 "__SHA_LINUX_X86_64__"
    end
  end

  def install
    bin.install Dir["doma-v__VERSION__-*"].first => "doma"
  end

  test do
    system "#{bin}/doma", "version"
  end
end
